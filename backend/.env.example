# PostgreSQL (required for backend)
POSTGRES_DB=havenjob
POSTGRES_USER=postgres
POSTGRES_PASSWORD=
POSTGRES_HOST=127.0.0.1
POSTGRES_PORT=5432

# Optional: use PostgreSQL in tests (default: SQLite)
# DJANGO_USE_POSTGRES_FOR_TESTS=1

# LLM / AI (used by apps.ai)
# LLM_PROVIDER=openai
# OPENAI_API_KEY=
# ANTHROPIC_API_KEY=

# Optional: override Django secret (default is dev-only)
# SECRET_KEY=your-secret-key
